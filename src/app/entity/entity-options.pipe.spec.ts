import { EntityOptionsPipe } from './entity-options.pipe';

describe('EntityOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
