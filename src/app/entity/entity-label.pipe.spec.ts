import { EntityLabelPipe } from './entity-label.pipe';

describe('EntityLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
